server:
  port:
    8765

spring:
  application:
    name:
      gateway

  cloud:
    gateway:
      routes:
        - id: clients
          uri: http://localhost:8081/
          predicates:
            - Path=/clients/**
        - id: storage
          uri: http://localhost:8082/
          predicates:
            - Path=/storage/**
#        - id: auth
#          uri: http://localhost:/
#          predicates:
#            - Path=/auth/**
server:
  port:
    8081

spring:
  application:
    name:
      clients

  datasource:
    url: jdbc:postgresql://localhost:55432/clients_db
    username: user
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


#management:
#  endpoints:
#    web:
#      exposure:
#        include: "*"
#  metrics:
#    export:
#      prometheus:
#        enable: true

